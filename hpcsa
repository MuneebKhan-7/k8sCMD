#!/usr/bin/env bash
# hpcsa — thin launcher that always uses the project's venv
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/.venv/bin/python" "$SCRIPT_DIR/hpcsa.py" "$@"

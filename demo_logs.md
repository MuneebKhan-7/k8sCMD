# k8sCMD Demo Logs

This document tracks the prompts, expected outcomes, and actual execution results for all the demonstrations and test cases in the Project HPCSA.

| Experiment Name | Prompt | Expected Output | Actual Output | Status | System Suggestion (if failed) | Retries Taken |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1. List Resources** | `List all nodes and pods running in the production namespace` | Returns a list of nodes and a list of pods in the production namespace. | Listed nodes and pods exactly as expected. | Success | N/A | 1 |
| **2. Deploy & Verify Pod** | `Deploy a Redis pod named redis-cache in the production namespace and verify it is running` | Creates `redis-cache` pod, then runs `get pods` to verify it exists and is running. | Verified creation and Running phase of `redis-cache`. | Success | N/A | 1 |
| **3. Create Deployment** | `Create a deployment called web-app using the nginx image in the production namespace with 1 replica` | Successfully triggers deployment creation for `web-app`. | Successfully created deployment `web-app` using `nginx` image. | Success | N/A | 1 |
| **4. Delete Resource** | `Delete the nginx-pod from the production namespace and confirm it no longer exists` | Deletes `nginx-pod`, then calls `get pods` to verify it is gone. | Confirmed `nginx-pod` is not found and does not appear in the production namespace. | Success/Retried | Run `kubectl delete pod nginx-pod -n production --ignore-not-found` | 2+ |
| **5. Deploy & Ingress** | `Deploy a pod named api-server using the httpd image in production, then create an ingress called api-ingress that routes traffic to it` | Creates `api-server` pod, then successfully maps `api-ingress` to it. | Deployed `api-server` pod and created ingress `api-ingress` successfully. | Success | N/A | 1 |
| **6. Cross-Namespace Awareness** | `List all pods in the 'development' namespace and then list all pods in the 'staging' namespace.` | Queries both namespaces sequentially; summarizes that they are empty (or lists their contents). | Confirmed both namespaces have no pods. | Success | N/A | 1 |
| **7. Sequential State Mutation** | `Delete the deployment named 'v1-app' in the 'production' namespace, and replace it by creating a new deployment named 'v2-app' using the 'nginx:alpine' image in the same namespace.` | Attempt to delete fails (not found), then correctly creates `v2-app`. (Due to mock restrictions on `--ignore-not-found`, this usually exhausts retries but finishes the creation). | Failed first attempt to delete. Mock API failed to ignore not-found, but `v2-app` creation succeeded. | Failed | Run `kubectl delete deployment v1-app -n production --ignore-not-found=true` | 3 |
| **8. Multi-Component Interdependency** | `Deploy a backend pod named 'auth-service' using the 'redis' image in the 'secure' namespace. Once running, create an ingress named 'auth-ingress' that routes traffic to it.` | Pod is running in `secure` namespace and ingress is successfully created. | Successfully deployed pod and created mapped ingress in `secure` namespace. | Success | N/A | 1 |
| **9. Full Lifecycle Pipeline** | `Create a deployment called 'temp-worker' with the 'busybox' image in the 'batch' namespace. Verify it exists by listing the deployments in that namespace, and then immediately delete it.` | Deployment is created, successfully listed in the output, and then deleted in one pipeline run. | Successfully created, verified, and deleted deployment `temp-worker` in `batch`. | Success | N/A | 1 |
| **10. Basic Self-Healing Loop** | `Try to delete a pod named 'missing-data-pod' in the 'production' namespace. When it fails, seamlessly recover by deploying a new pod named 'fallback-pod' using the 'nginx' image instead.` | Deletion explicitly fails (404), execution loop intercepts error, and falls back to create `fallback-pod`. | Initial delete failed due to missing pod, correctly fell back to creating `fallback-pod`. | Failed first step (Expected) | Execute fallback deployment since pod was not found. | 2 |
| **11. The "Clean-up Failure"** | `Try to delete a pod named 'ghost-pod' in the 'production' namespace. When that operation fails because the pod doesn't exist, recover your process by skipping the deletion and just listing the nodes in the cluster instead.` | Fails to delete, Validator catches it, pipeline pivots to simply executing `kubectl get nodes`. | Intercepted failure of `ghost-pod` deletion and pivoted safely to getting nodes. | Failed first step (Expected) | Skip deletion, pivot to query nodes. | 2 |
| **12. The "Unsupported Command" Pivot** | `Scale the deployment 'web-server' to 5 replicas. Since scaling isn't a supported command in our mock API, the system will throw an 'Unsupported' error. When you catch that error, pivot to creating a new deployment named 'web-server-v2' using the 'httpd' image.` | The `ExecutorAgent` catches an explicit unsupported error, `ValidatorAgent` processes the error and pivots to `create deployment`. | Recognized `scale` was not run, bypassed it and successfully generated `web-server-v2`. | Failed first step (Expected) | Skip scale operation, execute creation fallback. | 2 |
